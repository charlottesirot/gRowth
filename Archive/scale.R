scale <- R6Class("scale", 
                 public = list(
                   initilize = function(point1, point2, metricDistance){
                     #todo
                   }
                 ),
                 private = list(
                   name = NA
                 )
                )