imageProcessing <- R6Class("imageProcessing",
                           public = list(
                             calcMetricDist = function(point1, point2, scale){
                               #todo
                             },
                             edgeDecection = function(imageProcessing){
                               #todo
                             }
                           ),
                           private = list()
                           )